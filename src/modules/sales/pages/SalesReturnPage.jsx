import { ComingSoon } from "../../../components";

export default function SalesReturnPage() {
  return (
    <ComingSoon
      title="Sales Return"
      subtitle="Returns against sales invoices"
      description="Record goods returned by customers against a sales invoice, adjust stock, and issue a credit. Full workflow coming next."
    />
  );
}
